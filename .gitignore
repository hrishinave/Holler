# Secrets — never commit
.env
*.env
!.env.example

# Local handoff / planning doc — not for the public repo
CLAUDE.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# SQLite (later phases)
*.db
*.sqlite3
*.db-wal
*.db-shm
*.bak-*
# Runtime data dir (DB + WAL sidecars + backups)
server/data/

# Node / client (React/Next onboarding UI)
node_modules/
client/.next/
client/dist/
client/build/

# Editor / OS
.DS_Store
.idea/
.vscode/
